const CONFIG = {
  owner: 'shileiyuan',
  repo: 'shileiyuan.github.io'
  // owner: 'cobish',
  // repo: 'cobish.github.io'
};

export const Project_Path = '/';
export const issueUrl = `https://api.github.com/repos/${CONFIG.owner}/${CONFIG.repo}/issues`;